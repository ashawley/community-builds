// https://github.com/tek/splain.git#master

vars.proj.splain: ${vars.base} {
  name: "splain"
  uri: "https://github.com/tek/splain.git"

  extra.commands: [
    "set every gpgWarnOnFailure := true"
  ]
}
